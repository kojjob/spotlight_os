<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Spotlight OS - Voice AI Sales Platform" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : 'Transform your sales process with AI-powered voice assistants that qualify leads, book appointments, and engage customers 24/7.' %>">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="utf-8">
    <meta name="author" content="Spotlight OS">
    <meta name="robots" content="index, follow">
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= request.original_url %>">
    <meta property="og:title" content="<%= content_for?(:title) ? yield(:title) : 'Spotlight OS - Voice AI Sales Platform' %>">
    <meta property="og:description" content="<%= content_for?(:description) ? yield(:description) : 'Transform your sales process with AI-powered voice assistants' %>">
    <meta property="og:image" content="<%= asset_url('spotlight-og-image.png') %>">

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:url" content="<%= request.original_url %>">
    <meta property="twitter:title" content="<%= content_for?(:title) ? yield(:title) : 'Spotlight OS - Voice AI Sales Platform' %>">
    <meta property="twitter:description" content="<%= content_for?(:description) ? yield(:description) : 'Transform your sales process with AI-powered voice assistants' %>">
    <meta property="twitter:image" content="<%= asset_url('spotlight-og-image.png') %>">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link rel="icon" type="image/png" sizes="32x32" href="/icon.png">
    <link rel="icon" type="image/svg+xml" href="/icon.svg">
    
    <!-- Preload critical fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    
    <!-- Additional styles for content -->
    <%= yield(:head) if content_for?(:head) %>
  </head>

  <body class="bg-white text-gray-900 antialiased">
    <!-- Navigation -->
    <%= render 'shared/navbar' if lookup_context.exists?('shared/navbar') %>
    
    <!-- Main Content -->
    <main>
      <%= yield %>
    </main>
    
    <!-- Footer -->
    <%= render 'shared/footer' if lookup_context.exists?('shared/footer') %>
    
    <!-- Flash Messages -->
    <% if notice %>
      <div class="fixed top-4 right-4 z-50 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded shadow-lg" role="alert" data-controller="flash" data-flash-auto-hide-value="true">
        <span class="block sm:inline"><%= notice %></span>
        <span class="absolute top-0 bottom-0 right-0 px-4 py-3 cursor-pointer" data-action="click->flash#hide">
          <svg class="fill-current h-6 w-6 text-green-500" role="button" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20">
            <title>Close</title>
            <path d="M14.348 14.849a1.2 1.2 0 0 1-1.697 0L10 11.819l-2.651 3.029a1.2 1.2 0 1 1-1.697-1.697l2.758-3.15-2.759-3.152a1.2 1.2 0 1 1 1.697-1.697L10 8.183l2.651-3.031a1.2 1.2 0 1 1 1.697 1.697l-2.758 3.152 2.758 3.15a1.2 1.2 0 0 1 0 1.698z"/>
          </svg>
        </span>
      </div>
    <% end %>
    
    <% if alert %>
      <div class="fixed top-4 right-4 z-50 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded shadow-lg" role="alert" data-controller="flash" data-flash-auto-hide-value="true">
        <span class="block sm:inline"><%= alert %></span>
        <span class="absolute top-0 bottom-0 right-0 px-4 py-3 cursor-pointer" data-action="click->flash#hide">
          <svg class="fill-current h-6 w-6 text-red-500" role="button" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20">
            <title>Close</title>
            <path d="M14.348 14.849a1.2 1.2 0 0 1-1.697 0L10 11.819l-2.651 3.029a1.2 1.2 0 1 1-1.697-1.697l2.758-3.15-2.759-3.152a1.2 1.2 0 1 1 1.697-1.697L10 8.183l2.651-3.031a1.2 1.2 0 1 1 1.697 1.697l-2.758 3.152 2.758 3.15a1.2 1.2 0 0 1 0 1.698z"/>
          </svg>
        </span>
      </div>
    <% end %>
  </body>
</html>